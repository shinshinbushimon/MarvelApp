import React from "react";
import styled from "styled-components";
import { CheckboxProps } from "src/type/app";
import { InputField } from "../Input/BaseInputField";

/*
const Checkbox: React.FC<CheckboxProps> = ({ label, checked, onChange }) => (
    <label>
        <SChecKInput type="checkbox" checked={checked} onChange={onChange} />
        {label}
    </label>
);

const SChecKInput = styled(InputField)`

`;
*/